#include <bits/stdc++.h>
using namespace std;

int main() {
	int n, k;

	cin >> n >> k;
	vector< pair<int, int> > data(n);
	for (int i = 0; i < n; ++i)
		cin >> data[i].first >> data[i].second;

	

	return 0;
}